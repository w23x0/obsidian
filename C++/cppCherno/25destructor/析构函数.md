析构函数是类的一个特殊成员函数
在对象被销毁时自动调用

用于清理资源、释放内存或进行反初始化操作，防止内存泄漏
栈对象
堆对象

函数名与类名相同，但前面加上波浪号（~）


对象生命周期（Lifecycle） 和 作用域（Scope） 的核心概念
变量 e 是一个main函数直接定义的局部变量（栈对象）
析构函数的触发时机：局部变量只有在它所属的作用域（即 { } 大括号）结束时，才会被销毁
生命周期由它所在的作用域决定
return0直接结束
std::cin.get()等待输入结束

